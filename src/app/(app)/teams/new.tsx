import { Text, View } from 'react-native';

export default function AddTeamScreen() {
  return (
    <View className="bg-background flex-1 items-center justify-center">
      <Text className="text-foreground text-lg">Add Team</Text>
    </View>
  );
}